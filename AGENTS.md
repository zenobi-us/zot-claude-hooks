## Agent skills

### Issue tracker

Use local Markdown issues under `.scratch/`. See `docs/agents/issue-tracker.md` relative to the active Matt Pocock alignment root.

### Domain docs

Use a single project context with `CONTEXT.md` and `docs/adr/` at the alignment root. See `docs/agents/domain.md` relative to the active Matt Pocock alignment root.
